[alembic]
script_location = alembic
# Make direct Alembic CLI use from the backend checkout work as documented by
# Alembic. The container invokes Alembic as a module for the same reason.
prepend_sys_path = .
sqlalchemy.url = sqlite:///./pathly.db
